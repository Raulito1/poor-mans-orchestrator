Python Debugging Guardrails Prompt (FastAPI + Python 3.13 + Poetry)

Use this prompt when diagnosing API bugs, async issues, data inconsistencies, performance regressions, or production incidents in this repository.

---

You are debugging a Python 3.13 FastAPI service managed with Poetry.
Follow these guard rails for every debugging task.

Primary goals:
1. Reproduce first, then fix.
2. Prove root cause with evidence.
3. Keep fixes minimal and architecture-consistent.
4. Protect API contracts, async correctness, and observability hygiene.
5. Prevent recurrence with targeted tests.

Required debugging workflow:
1. Triage and reproduce:
- Capture endpoint, method, payload/query/path params, auth context, and environment.
- Record exact expected vs actual behavior, including status codes and error bodies.
- Reproduce with deterministic steps (test, script, or request collection).

2. Isolate failure domain:
- API layer: request parsing, dependency injection, response model mismatches.
- Service/domain layer: business rule or state-transition errors.
- Data access layer: query/filter/pagination/transaction issues.
- Integration layer: external API/client timeouts, retries, schema drift.
- Runtime layer: async blocking, connection pooling, concurrency/race conditions.

3. Instrument safely:
- Add targeted structured logs with correlation/request IDs where available.
- Log boundary events (request received, dependency resolution, service decisions, external calls, DB interactions).
- Never log secrets, tokens, credentials, or sensitive PII payloads.
- Remove temporary debug noise before finalizing.

4. Async and FastAPI-specific checks:
- Verify `async`/`await` usage is consistent for I/O paths.
- Ensure no blocking synchronous heavy work is executed in async request paths.
- Validate dependency lifecycles and resource reuse (DB sessions, HTTP clients).
- Confirm Pydantic request/response models match actual payload shape.

5. Root-cause proof:
- Demonstrate failing behavior with a test or reproducible request.
- Show why observed failure maps to specific module/function/query/config.
- Confirm adjacent flows are unaffected by the same trigger.

6. Implement minimal fix:
- Reuse existing routers/schemas/services/repositories/helpers.
- Keep route handlers thin and logic in service/domain layers.
- Keep error handling consistent with existing exception mapping patterns.
- Maintain Python typing quality; avoid `Any` shortcuts.

7. Regression hardening:
- Add/update tests:
  - Unit tests for service/domain logic.
  - API/integration tests for endpoint contract and status mapping.
  - Async behavior tests when issue involved concurrency or timing.
- Add edge-case coverage for the triggering conditions.

8. Validate and close:
- Re-run reproduction and confirm expected behavior.
- Verify related endpoints and shared dependencies for regressions.
- Keep dependency workflow Poetry-compliant if package changes were required.

Debugging checklist before finalizing:
- Did I reproduce before editing code?
- Did I prove root cause with concrete evidence?
- Is the fix minimal and architecture-aligned?
- Are async paths non-blocking and dependency lifecycles correct?
- Are API contracts/status codes/errors consistent?
- Are logs safe (no sensitive data)?
- Did I add/update tests to prevent recurrence?

Output requirements for each debugging task:
1. Reproduction summary:
- Endpoint/input/auth/environment.
- Expected vs actual behavior.

2. Root cause:
- Specific router/service/repository/dependency/config identified.
- Trigger conditions and failure mechanism.

3. Fix summary:
- Files/functions changed and why.
- Why this is the minimal safe fix.

4. Validation evidence:
- Tests added/updated and what they cover.
- Manual/automated checks run.
- Residual risks (if any).

If reproduction is incomplete or environment-specific, stop and request missing details (sample request, headers, auth context, env diffs, stack traces, timing data).
